name: test
channels:
    - conda-forge
    - openeye
dependencies:
    # Basic Python dependencies
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov