# minimum requirements to package and upload to PyPI
name: build-env
channels:
  - conda-forge
dependencies:
  # Basic Python dependencies
  - python>=3.11
  - pip

  # Unit testing
  - build
  - twine
  - sphinx